{
  "name": "speechify-mcp",
  "version": "0.1.0",
  "description": "Model Context Protocol server that exposes the Speechify text-to-speech API as tools.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "speechify-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@speechify/api": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
